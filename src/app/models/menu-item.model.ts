// src/app/models/menu-item.model.ts
export interface MenuItem {
    name: string;
    price: number;
    description: string;
    imageUrl?: string; // Opcional, para las imágenes de los artículos
  }
  